{{#each playlist.tracks}}
  <tr>
    <td>{{title}}</td>
    <td>{{artist}}</td>
    <td>{{duration}}</td>
    <td>
      <a
        href="/playlist/{{../playlist._id}}/track/{{_id}}/delete"
        class="button is-danger is-small"
      >
        Delete
      </a>
    </td>
  </tr>
{{/each}}
